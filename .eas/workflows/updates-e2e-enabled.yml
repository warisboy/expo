name: Updates E2E enabled
defaults:
  tools:
    yarn: '1.22.22'

on:
  push:
    branches: ['doug/sdk-52-workflow-testing']

jobs:
  Check paths:
    outputs:
      result: ${{ steps.changed_paths.outputs.result }}
    runs_on: linux-large-nested-virtualization
    image: latest
    steps:
      - uses: eas/checkout
      - uses: eas/install_node_modules
      - name: Check changed paths
        id: changed_paths
        run: |
          set-output result `yarn --silent ts-node --transpile-only ./packages/expo-updates/e2e/setup/check-changed-paths.ts sdk-52 packages/expo-updates`
      - name: Print result
        run: echo ${{ steps.changed_paths.outputs.result }}
  
#  iOS:
#    runs_on: macos-large
#    image: latest
#    steps:
#      - name: Install applesimutils
#        id: ios_simulator
#        env:
#          HOMEBREW_NO_AUTO_UPDATE: "1"
#        run: |
#          /opt/homebrew/bin/brew tap wix/brew
#          /opt/homebrew/bin/brew install applesimutils
#          xcrun simctl list
#      - uses: eas/checkout
#      - uses: eas/use_npm_token
#      - uses: eas/install_node_modules
#      - name: Set up Updates E2E enabled project
#        id: setup
#        env:
#          UPDATES_HOST: localhost
#          UPDATES_PORT: "4747"
#        run: |
#          yarn --silent ts-node --transpile-only ./packages/expo-updates/e2e/setup/create-eas-project.ts
#          ls -la ../updates-e2e
#      - name: Build E2E project
#        id: build
#        working_directory: ../updates-e2e
#        run: |
#          yarn generate-test-update-bundles
#          npx pod-install
#          npx detox build -c ios.debug
#      - name: Test E2E project
#        id: test
#        env:
#          UPDATES_HOST: localhost
#          UPDATES_PORT: "4747"
#        working_directory: ../updates-e2e
#        run: |
#          npx detox test -c ios.debug
#  Android:
#    runs_on: linux-large-nested-virtualization
#    steps:
#      - name: Set up JDK 17 and Android dependencies
#        id: setuptools
#        env:
#          JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
#        run: |
#          sudo apt-get --quiet update --yes
#          sudo apt-get --quiet install openjdk-17-jdk openjdk-17-jre libc6 libdbus-1-3 libfontconfig1 libgcc1 libpulse0 libtinfo5 libx11-6 libxcb1 libxdamage1 libnss3 libxcomposite1 libxcursor1 libxi6 libxext6 libxfixes3 zlib1g libgl1 pulseaudio socat --yes
#      - uses: eas/start_android_emulator
#        with:
#          device_name: pixel_4
#      - uses: eas/checkout
#      - uses: eas/use_npm_token
#      - uses: eas/install_node_modules
#      - name: Set up Updates E2E enabled project
#        id: setup
#        env:
#          UPDATES_HOST: localhost
#          UPDATES_PORT: "4747"
#        run: |
#          yarn --silent ts-node --transpile-only ./packages/expo-updates/e2e/setup/create-eas-project.ts
#          ls -la ../updates-e2e
#      - name: Build E2E project
#        id: build
#        working_directory: ../updates-e2e
#        env:
#          JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
#        run: | 
#          yarn generate-test-update-bundles
#          npx detox build -c android.debug
#      - name: Test E2E project
#        id: test
#        env:
#          JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
#          UPDATES_HOST: localhost
#          UPDATES_PORT: "4747"
#        working_directory: ../updates-e2e
#        run: |
#          yarn check-android-emulator
#          npx detox test -c android.debug
#
#
